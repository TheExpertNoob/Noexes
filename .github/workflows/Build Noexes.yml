name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container: devkitpro/devkita64:latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Build sysmodule
      run: make
      
    - name: Prepare release structure
      run: |
        cp noexs.nsp release_files/atmosphere/contents/054e4f4558454000/exefs.nsp
        
    - name: Create release archive
      run: |
        cd release_files
        zip -r ../noexes-sysmodule.zip atmosphere/
        cd ..
        
    - name: Upload build artifact
      uses: actions/upload-artifact@v4
      with:
        name: noexes-sysmodule
        path: noexes-sysmodule.zip
        
    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: noexes-sysmodule.zip
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
